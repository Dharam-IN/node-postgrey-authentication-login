import React from "react";

const Login = (props)=>{
    return(
        <>
            <h1>Login</h1>
            <button type="button" onClick={()=>{setAuth}}>Click</button>
        </>
    )
}

export default Login;
